# Copy to secrets.toml (local) or paste into Streamlit Cloud → Settings → Secrets
# Never commit secrets.toml to GitHub.

SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
SUPABASE_KEY = "your-publishable-or-anon-key"
